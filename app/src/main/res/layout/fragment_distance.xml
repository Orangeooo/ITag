<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.distance.DistanceFragment">

    <Button
        android:id="@+id/button_distance_open"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="24dp"
        android:text="OPEN"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/button_distance_clear"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="60dp"
        android:layout_marginTop="24dp"
        android:text="CLEAR"
        app:layout_constraintStart_toEndOf="@+id/button_distance_open"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/editText_distance"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="32dp"
        android:layout_weight="1"
        android:focusableInTouchMode="false"
        android:gravity="left|top"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/button_distance_open" />

    <TextView
        android:id="@+id/textView_distance_info"
        android:layout_width="248dp"
        android:layout_height="94dp"
        android:text="状态"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/editText_distance" />

</androidx.constraintlayout.widget.ConstraintLayout>